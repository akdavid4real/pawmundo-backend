export declare const RedisConfig: (() => {
    host: string;
    port: number;
    password: string;
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    host: string;
    port: number;
    password: string;
}>;
